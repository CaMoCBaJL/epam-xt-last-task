@using WebPL.Models;
@using Dependencies;
@using DataValidator;

@{
    Layout = "~/Pages/Layout/_BasicLayout.cshtml";

    if (DependencyResolver.Instance.BL.RecipeLogic.RemoveEntity(EntityToDeleteData.EntityId))
    {
        EntityToDeleteData.DeletionResult = Constants.allOk;
    }
    else
    {
        EntityToDeleteData.DeletionResult = Constants.serverError;
    }

    EntityToDeleteData.ConfirmationResult = false;

    Response.Redirect(EntityToDeleteData.SourcePage);
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <div>
    
    </div>
</body>
</html>
